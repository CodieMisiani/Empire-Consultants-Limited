import { z } from "zod";
export const leadSchema = z.object({ name: z.string().trim().min(2).max(100), email: z.string().trim().email().max(255), phone: z.string().trim().max(40).optional().or(z.literal("")), message: z.string().trim().max(3000).optional().or(z.literal("")), source: z.enum(["inquiry", "appointment", "event_registration", "contact"]), eventId: z.string().uuid().optional().nullable(), website: z.string().max(0).optional() });
export const serviceSchema = z.object({ title: z.string().trim().min(2).max(150), description: z.string().trim().min(10).max(10000), image_url: z.string().url().nullable().optional(), display_order: z.number().int().min(0), status: z.enum(["draft", "published"]) });
export const eventSchema = z.object({ title: z.string().trim().min(2).max(180), slug: z.string().trim().min(2).max(180).regex(/^[a-z0-9-]+$/), event_date: z.string().date(), event_time: z.string().max(40).nullable().optional(), location: z.string().trim().min(2).max(200), description: z.string().trim().min(10).max(10000), image_url: z.string().url().nullable().optional(), featured: z.boolean(), status: z.enum(["draft", "published"]) });
