import { PageHero } from "@/components/page-hero";import { LeadForm } from "@/components/lead-form";
export default async function Apply({searchParams}:{searchParams:Promise<{country?:string;service?:string;context?:string}>}){const p=await searchParams;const context=[p.context,p.country&&`Country: ${p.country}`,p.service&&`Service: ${p.service}`].filter(Boolean).join(" · ");return <><PageHero eyebrow="Apply now" title="Tell us about your ambitions.">Our team will help you define the strongest route forward.</PageHero><section className="section shell" style={{maxWidth:760}}><div className="inquiry-intro"><p className="eyebrow">A thoughtful starting point</p><p className="lead">The more we understand your goals, the more useful our first response can be.</p>{context&&<p className="inquiry-context">Your selected direction: {context}</p>}</div><div className="card form-surface"><LeadForm source="inquiry" context={context}/></div></section></>}
