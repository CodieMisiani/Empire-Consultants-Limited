import Image from "next/image";import Link from "next/link";import { PageHero } from "@/components/page-hero";import { fallbackEvents } from "@/lib/content";
export default function Events(){return <><PageHero eyebrow="Empire events" title="Meet your future, in person.">Join our university open days, education fairs and expert sessions.</PageHero><section className="section shell"><div className="grid grid-2">{fallbackEvents.map(e=><article className="card" key={e.id} style={{overflow:"hidden"}}>{e.image_url&&<Image src={e.image_url} alt="" width={800} height={420} style={{width:"100%",height:250,objectFit:"cover"}}/>}<div style={{padding:24}}><p className="eyebrow">{new Intl.DateTimeFormat("en",{dateStyle:"medium"}).format(new Date(e.event_date))} · {e.location}</p><h2 className="heading" style={{fontSize:"1.7rem",margin:"10px 0"}}>{e.title}</h2><p className="prose">{e.description}</p><Link className="button button-primary" href={`/events/${e.slug}`}>Register now</Link></div></article>)}</div></section></>}
