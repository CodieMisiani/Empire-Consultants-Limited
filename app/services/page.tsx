import { PageHero } from "@/components/page-hero";
import { fallbackServices } from "@/lib/content";
import Link from "next/link";
export default function Services(){return <><PageHero eyebrow="What we do" title="A clear path to your global future.">End-to-end advice for international education, visa preparation and long-term career direction.</PageHero><section className="section shell"><div className="grid grid-3">{fallbackServices.map((service)=><article className="card" key={service.id} style={{padding:32,display:"flex",flexDirection:"column"}}><p className="eyebrow">Empire expertise</p><h2 className="heading" style={{fontSize:"1.6rem",margin:"14px 0"}}>{service.title}</h2><p className="prose" style={{flex:1}}>{service.description}</p><Link href={`/apply?service=${encodeURIComponent(service.title)}`} className="button button-outline">Apply now</Link></article>)}</div></section></>}
