import { PageHero } from "@/components/page-hero";
import { fallbackServices } from "@/lib/content";
import Link from "next/link";
import { EditorialCta } from "@/components/editorial-cta";
export default function Services(){return <><PageHero eyebrow="What we do" title="A clear path to your global future.">End-to-end advice for international education, visa preparation and long-term career direction.</PageHero><section className="section shell"><div className="section-heading"><p className="eyebrow">One team, three connected disciplines</p><h2 className="heading">Advice that holds together from first choice to final departure.</h2><hr className="editorial-rule" /></div><div className="grid grid-3" style={{marginTop:40}}>{fallbackServices.map((service,index)=><article className="card" key={service.id} style={{padding:32,display:"flex",flexDirection:"column"}}><p className="eyebrow">0{index+1} · Empire expertise</p><h2 className="heading" style={{fontSize:"1.6rem",margin:"14px 0"}}>{service.title}</h2><p className="prose" style={{flex:1}}>{service.description}</p><Link href={`/apply?service=${encodeURIComponent(service.title)}`} className="button button-outline">Explore this service</Link></article>)}</div></section><EditorialCta title="Bring us the question you are carrying.">Whether you have a clear shortlist or are just beginning to explore, we will help you decide what matters next.</EditorialCta></>}
