import Link from "next/link";import { PageHero } from "@/components/page-hero";
const countries=["Canada","United Kingdom","Australia","United States","Germany","Ireland"];
export default function Countries(){return <><PageHero eyebrow="Global destinations" title="Find a country that feels like home.">Explore leading study destinations with opportunities that match your academic and career aspirations.</PageHero><section className="section shell"><div className="grid grid-3">{countries.map((country,i)=><article className="card" key={country} style={{minHeight:190,padding:24,background:i%2?"var(--soft)":"white"}}><p className="eyebrow">Study in</p><h2 className="heading" style={{fontSize:"1.7rem",margin:"12px 0 24px"}}>{country}</h2><Link href={`/apply?country=${country}`} style={{color:"var(--gold)",fontWeight:700}}>Apply now →</Link></article>)}</div></section></>}
